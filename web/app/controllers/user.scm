;; Controller user definition of web
;; Please add your license header here.
;; This file is generated automatically by GNU Artanis.
(define-artanis-controller user) ; DO NOT REMOVE THIS LINE!!!

(user-define show
  (lambda (rc)
  "<h1>This is user#show</h1><p>Find me in app/views/user/show.html.tpl</p>"
  ;; TODO: add controller method `show'
  ;; uncomment this line if you want to render view from template
  ;; (view-render "show" (the-environment))
  ))

(user-define edit
  (lambda (rc)
  "<h1>This is user#edit</h1><p>Find me in app/views/user/edit.html.tpl</p>"
  ;; TODO: add controller method `edit'
  ;; uncomment this line if you want to render view from template
  ;; (view-render "edit" (the-environment))
  ))

